{
  "name": "url-shortener-microservice",
  "version": "0.0.1",
  "description": "Backend microservice that creates and stores a stortened url that can be used as an alias for the full URL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.12.4",
    "mongodb": "*",
    "valid-url": "^1.0.9"
  },
  "engines": {
    "node": "6.9.4"
  },
  "repository": {
    "url": "https://github.com/dickyw71/url-shortener-microservice"
  },
  "keywords": [
    "node",
    "express",
    "mongodb",
    "database"
  ],
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-http": "^3.0.0",
    "mocha": "^3.2.0"
  }
  "scripts": {
    "test": "mocha --timeout 10000"
  }
}
